{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "launch.json",
            "type": "gdb",
            "request": "launch",
            "target": "${fileDirname}/${fileBasenameNoExtension}",
            "cwd": "${workspaceFolder}",
            "valuesFormatting": "parseText",
            "dap-compilation": "make"
        }
    ]
}
